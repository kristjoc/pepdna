# pep-dna

## Quick Start

**1. Install Dependencies**
```bash
sudo apt update
sudo apt install build-essential make automake autoconf linux-headers-$(uname -r)
```

**2. Build and Install**
```bash
./configure
make all
sudo -E make install
```
